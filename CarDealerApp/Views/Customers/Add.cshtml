@using CarDealerApp.Extentions
@using System.Collections
 
@model CarDealer.Models.EntityModels.User

<h2>Add Customer</h2>

<form method="post">
    <div class="form-group">
        <label for="name">Name: </label>
        <input type="text" id="name" name="name" />
    </div>
    <div class="form-group">
        <label for="birthdate">Birthdate: </label>
        <input type="date" id="birthdate" name="birthdate" />
    </div>
    <div class="form-group">
        <input type="submit" value="Submit" />
    </div>
</form>

@Html.Table("Customers", (IList)ViewBag.Users, null)

@*<table class="table table-striped">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Age)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsSubscribed)
        </th>
    </tr>

    @foreach (var item in (CarDealer.Models.EntityModels.User[])ViewBag.Users)
    {
        <tr>
            <td>

                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Age)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsSubscribed)
            </td>
        </tr>
    }

</table>*@
